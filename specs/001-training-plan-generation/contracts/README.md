# Contracts — CU-TRAIN-04

The actual OpenAPI YAML contracts are located in:

```
docs/product/Contract/Train-Service/CU-TRAIN-04/
├── API CONTRACT – CU-TRAIN-04 – Generate Plan.yaml
├── API CONTRACT – CU-TRAIN-04 – Get Active Plan.yaml
└── API CONTRACT – CU-TRAIN-04 – Get Plan Schedule.yaml
```

These files are the **source of truth** per Constitution Principle I and were created as part of this feature's Phase 1 design. Any future change to the three plan endpoints must update these YAMLs first.
